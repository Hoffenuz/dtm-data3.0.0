{
  "name": "dtmdata",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "dtmdata.uz — O'zbekiston OTMlari, DTM kirish ballari va qabul ma'lumotlari portali",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "generate:seo": "npm run generate:seo --prefix backend",
    "build": "npm run generate:seo && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "start:prod": "npm run build && npm run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
